'use strict'

export default class Voronoi
{
  static init(config)
  {
    return state
  }

  static update({ deltaTime: dt, appState: state })
  {
    return state
  }

  static draw({ appState: state })
  {

  }
}
